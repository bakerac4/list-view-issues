{{#if (eq @type "fad")}}
    <Label ...attributes text={{this.icon}} class={{this.type}} />
    <Label ...attributes text={{this.duoIcon}} class={{this.type}} opacity=".5" />
{{else}}
    <Label ...attributes text={{this.icon}} class={{this.type}} />
{{/if}}